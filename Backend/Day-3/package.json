{
  "name": "day-3",
  "version": "1.0.0",
  "description": "Middlewares :\r                Middle ware in express refers to the function that process before reaching the route handlers\r                These functions can modify req and res objects, end the req-res cycle or call next() middle ware function.\r                Middleware functions are executed in the order they defined.",
  "main": "middleware.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
